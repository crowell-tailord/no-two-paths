/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'www.tokyorebels.io',
            'cdn.midjourney.com'
        ]
    }
}

module.exports = nextConfig
