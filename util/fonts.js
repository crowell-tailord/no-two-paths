import { Stylish, Texturina } from 'next/font/google'

export const titleFont = Texturina({
    weight: ["700"],
    subsets: ["latin"]
});

export const textFont = Stylish({
    weight: ["400"],
    subsets: ["latin"]
});
